{
  "skill_name": "code-smellz",
  "evals": [
    {
      "id": 1,
      "prompt": "Run code-smellz on this codebase",
      "expected_output": "Standard mode dispatches only the 2-3 roles justified by pre-pass evidence, merges and triages findings, writes STAGED_CHANGES.md, applies scoped fixes without commits, runs available gates/tests, and emits a final report.",
      "files": [
        "src/",
        "tests/"
      ],
      "expectations": [
        "States the mode line before setup or dispatch",
        "Resolved WORKSPACE is created before any subagents are launched",
        "detect_runner.py and measure_loc.py are used during setup",
        "Optional static/dependency analysis is recorded when available",
        ".stinkyignore is checked and loaded if present",
        "Dependency CVE scanner is detected and run when available",
        "Standard mode launches only 2-3 roles justified by pre-pass evidence; all four are reserved for thorough mode",
        "Findings JSON files exist and contain valid JSON with patch fields where applicable",
        "STAGED_CHANGES.md is written before any edits are applied",
        "Triage summary is printed before changes are applied",
        "git diff checkpoint is saved before edits",
        "Patches are checked before application when patch files are used",
        "Type-checker gate runs if available before the test suite",
        "Linter gate runs if available",
        "Formatter gate runs if available",
        "Test suite is run and result is recorded",
        "Failed changes are reverted with git apply -R (not git commit/revert)",
        "No git commit is made at any point",
        "Session state tracks applied, skipped, and reverted findings",
        "Final report includes verification status",
        "DONE: signal emitted with iteration count and LOC delta"
      ]
    },
    {
      "id": 2,
      "prompt": "This code is getting pretty stinky, can you clean it up?",
      "expected_output": "Skill triggered by casual 'stinky' + 'clean up' language; mode is chosen via scaling and at least one safe cleanup pass runs when findings exist.",
      "files": [
        "src/"
      ],
      "expectations": [
        "Skill triggers on informal 'stinky' phrasing",
        ".stinkyignore is checked before source file enumeration",
        "Optional static analysis runs when available",
        "Dependency CVE scanner is detected and run if available",
        "Codebase goal is read from README or project manifest before spawning agents",
        "WORKSPACE/session.json records baseline and runner/check information",
        "Subagents receive available pre-computed analysis data",
        "At least one safe change is applied when findings exist",
        "STAGED_CHANGES.md is written before edits",
        "Type-check, lint, and format gates run (or noted as unavailable)",
        "No git commit is made",
        "Final LOC is measured",
        "Final report shows verification status and changes-applied count"
      ]
    },
    {
      "id": 3,
      "prompt": "Explain what this code does",
      "expected_output": "Skill does NOT trigger \u2014 code is explained without spawning subagents or modifying anything.",
      "files": [
        "src/"
      ],
      "expectations": [
        "Skill is NOT triggered",
        "No code-smellz WORKSPACE is created",
        "No static analysis is run",
        "No subagents are spawned",
        "No file edits are made",
        "Response is a plain explanation of the code's purpose and structure"
      ],
      "prompt_type": "anti_trigger"
    },
    {
      "id": 4,
      "prompt": "There's an off-by-one bug in parse_date() in dates.py \u2014 a single function, about 40 lines. Find and fix just that bug.",
      "expected_output": "Takes the lite tier: reviews the single function directly \u2014 no subagents spawned. A 40-line single-function bug fix meets none of the Hard Spawn Gate criteria (not parallel, no isolated context needed, not an independent workstream), so the crew is not spawned at all.",
      "expectations": [
        "States the mode line (mode: lite ...; budget: 0) before editing",
        "Takes the lite tier \u2014 spawns NO subagents for a single-function bug fix (meets no Hard Spawn Gate criterion)",
        "Reviews parse_date directly and fixes the off-by-one"
      ],
      "required_regex": [
        "mode:"
      ],
      "manual_checks": []
    },
    {
      "id": 5,
      "prompt": "Run code-smellz on this codebase --quick \u2014 fast pass, just the most relevant cleanup.",
      "expected_output": "The explicit --quick flag pins the lite tier regardless of codebase size: works directly or spawns only 1\u20132 relevant agents (not all four), one iteration, no quarantine loop. Still writes findings, applies safe changes, runs available gates, and emits a DONE: line.",
      "expectations": [
        "States the mode line (mode: lite (--quick); budget: ...) before cleanup",
        "The --quick flag is honored \u2014 lite tier pinned by the explicit flag, not by task-size auto-detection",
        "Does NOT spawn all four agents in parallel (direct work or 1\u20132 relevant agents only)",
        "No quarantine loop runs",
        "No git commit is made at any point",
        "Emits a DONE: signal with iteration count and LOC delta"
      ],
      "required_regex": [
        "mode:",
        "DONE:"
      ],
      "forbidden_regex": [
        "git commit"
      ],
      "manual_checks": []
    }
  ]
}
