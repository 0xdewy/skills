{
  "skill_name": "invariant-miner",
  "evals": [
    {
      "id": 1,
      "prompt_type": "forced",
      "prompt": "Mine the behavioral invariants implied by this tag canonicalizer and its contract, then add deterministic generative tests using the repository's existing test stack. Do not change production code.",
      "expected_output": "Activates invariant-miner, derives evidence-backed idempotence and metamorphic candidates, tests them with bounded deterministic cases, and records results without editing production code.",
      "expectations": [
        "States a mode line and resolves a scratch workspace",
        "Reads CONTRACT.md, the implementation, callers or tests, and test configuration",
        "Separates contract, derived, and observed bases",
        "Uses metamorphic relations such as permutation, duplication, or idempotence rather than copying implementation logic",
        "Records deterministic seed and case or time budgets",
        "Records exit code, executed case count, duration, and output SHA-256",
        "Adds only authorized test files and does not edit src/tags.py",
        "Writes and validates INVARIANTS.json plus REPORT.md",
        "Describes passing trials as not falsified within the budget rather than proven"
      ],
      "required_regex": ["INVARIANTS.json", "REPORT.md", "DONE:"],
      "forbidden_regex": ["git commit", "pip install", "proven correct"],
      "fixture_files": {
        "CONTRACT.md": "canonical_tags returns unique, lowercase, trimmed tags in deterministic order. Input ordering and duplicate entries are irrelevant.\n",
        "src/tags.py": "def canonical_tags(tags):\n    return sorted({tag.strip().lower() for tag in tags if tag.strip()})\n",
        "tests/test_tags.py": "import unittest\nfrom src.tags import canonical_tags\n\nclass TagTests(unittest.TestCase):\n    def test_example(self):\n        self.assertEqual(canonical_tags([' B ', 'a', 'a']), ['a', 'b'])\n"
      }
    },
    {
      "id": 2,
      "prompt": "Use invariant-miner --quick to discover and falsify candidate invariants for src/codec.py. Report only: keep all repository files unchanged.",
      "expected_output": "Uses quick mode with no agents, keeps probes in the resolved workspace, and reports bounded round-trip and representation candidates without durable repo edits.",
      "expectations": [
        "Uses quick mode with 3-5 candidates and zero subagents",
        "Keeps generated probes outside the repository source and test files",
        "Tries boundary or exhaustive values before random volume",
        "Reports the exact commands, seed, and budget",
        "Does not claim finite passing cases constitute proof"
      ],
      "forbidden_regex": ["git commit", "pip install"],
      "fixture_files": {
        "src/codec.py": "def encode(value: int) -> str:\n    return str(value)\n\ndef decode(value: str) -> int:\n    return int(value)\n"
      }
    },
    {
      "id": 3,
      "prompt": "Find hidden invariants in this ledger state machine and add stateful tests. Do not fix production code even if you find a counterexample.",
      "expected_output": "Derives conservation and failure-atomicity candidates from the contract, generates bounded operation sequences, minimizes the overdraft counterexample, and leaves production unchanged.",
      "outcome_expected": "Finds and minimizes the rejected-withdrawal state corruption, preserves it as a reproducible test, and leaves production code unchanged.",
      "outcome_expectations": [
        "Demonstrates that a rejected overdraft changes the balance",
        "Reports a minimal reproducible operation sequence",
        "Adds or supplies a deterministic regression test that fails for the current implementation",
        "Does not modify ledger.py"
      ],
      "expectations": [
        "Uses a stateful or model-based falsification method",
        "Tests valid, invalid, repeated, and boundary operations",
        "Finds and reproduces the rejected-withdrawal mutation bug",
        "Shrinks the failure to a minimal operation sequence",
        "Records a concrete reproduction command and failing execution receipt",
        "Marks the property falsified with a concrete counterexample",
        "Adds an authorized failing regression test but does not edit ledger.py"
      ],
      "fixture_files": {
        "CONTRACT.md": "Balances never become negative. A rejected withdrawal raises ValueError and leaves the balance unchanged.\n",
        "ledger.py": "class Ledger:\n    def __init__(self):\n        self.balance = 0\n\n    def deposit(self, amount):\n        if amount < 0:\n            raise ValueError('negative deposit')\n        self.balance += amount\n\n    def withdraw(self, amount):\n        self.balance -= amount\n        if self.balance < 0:\n            raise ValueError('overdraft')\n"
      }
    },
    {
      "id": 4,
      "prompt": "Run invariant-miner --fix on the ledger. Mine failure-atomicity invariants, preserve the minimized counterexample as a regression test, apply the smallest fix, and verify it.",
      "expected_output": "Produces the counterexample before editing, adds a regression test, minimally fixes withdrawal atomicity, and reruns targeted and relevant tests.",
      "expectations": [
        "Establishes a failing counterexample before changing production code",
        "Preserves the minimized case as a durable regression test",
        "Applies only the smallest production fix justified by the counterexample",
        "Reruns the new test and relevant suite",
        "Records pre-fix fail and post-fix pass evidence"
      ],
      "forbidden_regex": ["git commit", "git reset --hard"],
      "fixture_files": {
        "CONTRACT.md": "A rejected withdrawal leaves the balance unchanged.\n",
        "ledger.py": "class Ledger:\n    def __init__(self):\n        self.balance = 0\n\n    def withdraw(self, amount):\n        self.balance -= amount\n        if self.balance < 0:\n            raise ValueError('overdraft')\n"
      }
    },
    {
      "id": 5,
      "prompt": "The implementation preserves underscores in slugs but the public schema forbids them. Mine the invariant without changing files, and do not silently choose which source is authoritative.",
      "expected_output": "Records the conflicting claims and blocks the candidate pending authority instead of treating current behavior as a contractual invariant.",
      "expectations": [
        "Distinguishes the public contract from observed implementation behavior",
        "Marks the conflicting candidate blocked with an explicit reason",
        "Does not encode the observed quirk as a durable test",
        "Does not modify implementation, tests, or schema"
      ],
      "fixture_files": {
        "schema.json": "{\"slug\": {\"pattern\": \"^[a-z0-9-]+$\"}}\n",
        "slug.py": "def slugify(value):\n    return value.strip().lower().replace(' ', '-')\n"
      }
    },
    {
      "id": 6,
      "prompt_type": "anti_trigger",
      "prompt": "Add one unit test asserting that parse_port('8080') returns 8080.",
      "expected_output": "Does not invoke invariant-miner because this is one known example-based test; adds the narrow test directly.",
      "expectations": [
        "Does not create an invariant ledger or generative harness",
        "Adds the requested example test directly"
      ]
    },
    {
      "id": 7,
      "prompt_type": "anti_trigger",
      "prompt": "Run code-smellz across the repository and fix broad correctness, security, and maintainability problems.",
      "expected_output": "Routes to code-smellz instead of invariant-miner because the task is broad cleanup rather than falsifying general properties.",
      "expectations": [
        "Uses code-smellz rather than invariant-miner",
        "Does not force every finding into a property-test workflow"
      ]
    }
  ]
}
