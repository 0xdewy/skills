{
  "skill_name": "goal",
  "evals": [
    {
      "id": 1,
      "prompt": "Iterate until `pytest tests/` passes with exit 0 in this repo. Keep going until it's green; don't stop until the goal is met. VERIFY: pytest tests/",
      "expected_output": "Sets fixed criteria with the VERIFY command authoritative, records durable status, loops through attempt/evidence/diagnosis, optionally dispatches one scoped worker, and stops immediately when the command and all criteria pass.",
      "expectations": [
        "Derives fixed measurable success criteria during setup, with the VERIFY command as the authoritative gate",
        "Each iteration records a plan, attempt, a per-criterion verification table with cited evidence, and a verdict",
        "Uses the verify command's exit code as authoritative (a self-judged PASS cannot override a non-zero exit)",
        "Stops immediately when the command exits 0 and all criteria PASS",
        "Ends with a parseable DONE: line",
        "Does not git reset --hard, git checkout ., or git commit"
      ],
      "required_regex": [
        "DONE:"
      ],
      "forbidden_regex": [
        "git reset --hard",
        "git commit"
      ],
      "manual_checks": []
    },
    {
      "id": 2,
      "prompt": "Goal: every section of README.md must contain a runnable code example. Loop until that's true.",
      "expected_output": "A domain-agnostic goal run that derives one observable criterion per README section, works directly, and judges each criterion with quoted evidence until all pass or the cap is hit.",
      "expectations": [
        "Treats this as direct work rather than dispatching a worker for prose edits",
        "Each criterion is observable (an example exists in section X) and judged with quoted evidence",
        "No self-judged PASS without citing the actual example text",
        "Ends with a DONE: line that is PARTIAL if any section still lacks an example at the cap"
      ],
      "required_regex": [
        "DONE:"
      ],
      "forbidden_regex": [],
      "manual_checks": []
    },
    {
      "id": 3,
      "prompt": "Fix the typo on line 4 of utils.py.",
      "prompt_type": "anti_trigger",
      "expected_output": "Recognizes this as a single one-shot edit with no loop needed, and just makes the edit directly (possibly pointing out that the goal skill is overkill), rather than standing up a criteria/verify/iterate pipeline.",
      "expectations": [
        "Does not stand up a multi-iteration goal loop for a single typo",
        "Makes the one edit directly"
      ],
      "required_regex": [],
      "forbidden_regex": [],
      "manual_checks": []
    },
    {
      "id": 4,
      "prompt": "Implement this feature and iterate on it until reviewers are happy with the quality.",
      "expected_output": "Uses goal with an explicit review gate: implements, obtains one evidence-backed independent review, fixes concrete defects, and still runs objective acceptance checks.",
      "expectations": [
        "Declares review as one gate alongside objective criteria",
        "Gives the reviewer task, criteria, diff or artifact, and verification evidence",
        "Fixes only cited defects and reruns objective verification",
        "Does not treat reviewer approval as overriding a failed check"
      ],
      "required_regex": [],
      "forbidden_regex": [],
      "manual_checks": []
    },
    {
      "id": 5,
      "prompt": "Iterate until the build script prints 'BUILD OK'. VERIFY: ./build.sh | grep -q 'BUILD OK'",
      "expected_output": "A --quick-tier goal with a single clear criterion and an authoritative verify command. Works directly, caps at a small number of iterations, and stops as soon as the command exits 0.",
      "expectations": [
        "States the mode line (mode: --quick ...; budget: ...) before iterating",
        "Takes the --quick tier for a single-criterion goal with a verify command",
        "Does not dispatch workers for a trivial build fix (0 agents)",
        "Uses the verify command exit code as the sole gate",
        "Ends with a DONE: line"
      ],
      "required_regex": [
        "mode:",
        "DONE:"
      ],
      "forbidden_regex": [],
      "manual_checks": []
    },
    {
      "id": 6,
      "prompt": "Goal: make this module compile under strict mode with zero warnings. VERIFY: make strict 2>warnings.txt; test ! -s warnings.txt. Keep iterating until it passes.",
      "expected_output": "If the goal proves unreachable within the cap (e.g. a third-party dependency emits unavoidable warnings), the skill stops early per the anti-degeneration rules and emits a PARTIAL/BLOCKED DONE: line naming the unmet criterion and root cause, rather than claiming success or looping until the cap on identical attempts.",
      "expectations": [
        "On repeated identical failure, changes strategy or stops early rather than retrying identically",
        "Never emits a DONE: claiming success when the verify command exits non-zero",
        "At the cap or when blocked, emits PARTIAL or BLOCKED with the failing criterion and a one-line root cause"
      ],
      "required_regex": [
        "DONE:"
      ],
      "forbidden_regex": [],
      "manual_checks": []
    },
    {
      "id": 7,
      "prompt": "Resume this goal run from WORKSPACE=run. Do not repeat criteria already marked passed; continue from next_action until `sh verify.sh` exits zero.",
      "expected_output": "Validates the unfinished status and artifacts, resumes at iteration 3, preserves the passed parse criterion, and executes only the recorded next action and remaining verification.",
      "expectations": [
        "Reads and validates run/status.json before planning",
        "Runs the shared goal state validator",
        "Does not archive or restart the matching unfinished run",
        "Does not repeat the already passed parse criterion",
        "Continues from next_action and increments the iteration",
        "Updates durable state before the next attempt"
      ],
      "fixture_files": {
        "run/status.json": "{\"schema_version\":1,\"goal\":\"make verifier pass\",\"mode\":\"standard\",\"state\":\"in_progress\",\"cap\":8,\"gate_type\":\"command\",\"current_iteration\":2,\"criteria\":[{\"id\":\"parse\",\"state\":\"passed\",\"evidence\":\"iteration 2\"},{\"id\":\"verify\",\"state\":\"failed\",\"evidence\":\"exit 1\"}],\"attempts\":2,\"last_evidence\":\"exit 1\",\"blockers\":[],\"next_action\":\"fix serializer boundary\"}\n",
        "verify.sh": "#!/bin/sh\nexit 1\n"
      }
    }
  ]
}
