{
  "skill_name": "spec-reconciler",
  "evals": [
    {
      "id": 1,
      "prompt_type": "forced",
      "prompt": "Reconcile the user-creation contract across the OpenAPI schema, handler, and tests. Find contract drift and report it without changing files.",
      "expected_output": "Activates spec-reconciler, extracts atomic status-code claims with provenance, applies the repository-declared schema authority, and reports the implementation/test disagreement without editing primary surfaces.",
      "expectations": [
        "States the mode and resolves a scratch workspace",
        "Records AGENTS.md as authority evidence rather than assuming code or majority wins",
        "Extracts the 201 schema claim and 200 handler/test claims with path:line provenance",
        "Classifies the mismatch as a non-fixable cross-surface conflict",
        "Writes valid CLAIMS.json, CONFLICTS.json, and RECONCILIATION.md",
        "Leaves every fixture unchanged"
      ],
      "required_regex": ["CLAIMS.json", "CONFLICTS.json", "RECONCILIATION.md", "DONE:"],
      "forbidden_regex": ["git commit", "majority", "newest file"],
      "fixture_files": {
        "AGENTS.md": "# Contract policy\nThe public API contract in api/openapi.json is authoritative. Runtime code and tests must conform, but remain human-maintained primary surfaces.\n",
        "api/openapi.json": "{\"paths\":{\"/users\":{\"post\":{\"responses\":{\"201\":{\"description\":\"created\"}}}}}}\n",
        "server.py": "def create_user(request):\n    return {'id': 'u1'}, 200\n",
        "tests/test_users.py": "def test_create_user(client):\n    response = client.post('/users', json={'email': 'a@example.test'})\n    assert response.status_code == 200\n"
      }
    },
    {
      "id": 2,
      "prompt": "spec-reconciler --quick: compare the CLI parser and README for the deploy --environment default. There is no source-of-truth policy; report only.",
      "expected_output": "Uses quick mode, extracts two source-anchored default-value claims, leaves ranks null, and blocks the disagreement as ambiguous authority.",
      "expectations": [
        "Uses quick mode with zero subagents and no more than 20 claims",
        "Does not infer authority from executable code or file freshness",
        "Sets authority policy source and ranks to null",
        "Classifies the prod versus staging disagreement as ambiguous-authority",
        "Does not edit either file"
      ],
      "fixture_files": {
        "cli.py": "import argparse\nparser = argparse.ArgumentParser()\nparser.add_argument('--environment', default='prod')\n",
        "README.md": "Run `tool deploy --environment NAME`. When omitted, the environment defaults to `staging`.\n"
      }
    },
    {
      "id": 3,
      "prompt": "Run spec-reconciler --fix for the widget id type. The generated Python client is stale; use the recorded generator, then prove the reconciled claim and tests agree.",
      "expected_output": "Uses the declared OpenAPI authority and generator relationship, marks only the generated client fixable, regenerates it, re-extracts claims, and verifies no target drift remains.",
      "expectations": [
        "Records the OpenAPI policy and generated-client provenance",
        "Classifies the client mismatch as fixable stale-derived",
        "Runs python scripts/generate_client.py rather than hand-editing src/client.py",
        "Re-extracts affected claims after regeneration",
        "Reruns reconciliation and the relevant test",
        "Does not edit api/openapi.json or the generator"
      ],
      "forbidden_regex": ["git commit", "hand-edit"],
      "fixture_files": {
        "AGENTS.md": "# Generation policy\napi/openapi.json is authoritative. src/client.py is derived by `python scripts/generate_client.py` and must never be edited manually.\n",
        "api/openapi.json": "{\"components\":{\"schemas\":{\"Widget\":{\"properties\":{\"id\":{\"type\":\"string\"}}}}}}\n",
        "src/client.py": "# Generated file. Do not edit.\nWidgetId = int\n",
        "scripts/generate_client.py": "import json\nfrom pathlib import Path\nspec = json.loads(Path('api/openapi.json').read_text())\nkind = spec['components']['schemas']['Widget']['properties']['id']['type']\npy_type = {'string': 'str', 'integer': 'int'}[kind]\nPath('src/client.py').write_text(f'# Generated file. Do not edit.\\nWidgetId = {py_type}\\n')\n",
        "tests/test_client.py": "from src.client import WidgetId\n\ndef test_widget_id_type():\n    assert WidgetId is str\n"
      }
    },
    {
      "id": 4,
      "prompt": "Reconcile the required GraphQL Money fields with the response example. Report omissions but do not edit the example.",
      "expected_output": "Uses the explicit example-coverage policy to record an expectation and reports the missing currency claim as an omission, without treating all undocumented fields as speculative omissions.",
      "expectations": [
        "Extracts amount and currency requiredness from schema.graphql",
        "Uses AGENTS.md as evidence that examples must cover required fields",
        "Adds an expectation only for policy-covered required fields",
        "Reports the missing currency claim as an omission",
        "Does not edit docs/example.json"
      ],
      "fixture_files": {
        "AGENTS.md": "# Example policy\nGraphQL SDL is authoritative. Public response examples are derived and must include every non-null response field.\n",
        "schema.graphql": "type Money {\n  amount: Int!\n  currency: String!\n}\n",
        "docs/example.json": "{\"amount\": 1250}\n"
      }
    },
    {
      "id": 5,
      "prompt": "Determine the timeout unit across spec.md and config.schema.json. Both are declared primary with equal authority. Do not choose one for me.",
      "expected_output": "Captures the equal-authority millisecond and second claims and reports an authority conflict requiring a human decision.",
      "outcome_expected": "Reports both timeout-unit claims with provenance and correctly leaves the equal-authority conflict unresolved.",
      "outcome_expectations": [
        "Identifies milliseconds in spec.md and seconds in config.schema.json",
        "Cites both source files",
        "Does not invent an authority winner or edit either source",
        "States that a maintainer decision is required"
      ],
      "expectations": [
        "Automatically selects the inline quick path for this one-key report-only conflict",
        "Does not create a full reconciliation workspace or claim ledger",
        "Assigns both primary surfaces the same top authority rank",
        "Preserves the semantically different units without normalization",
        "Classifies the group as authority-conflict",
        "Marks it non-fixable and does not edit either primary surface"
      ],
      "fixture_files": {
        "AGENTS.md": "# Configuration policy\nspec.md and config.schema.json jointly define the public configuration contract with equal authority. Conflicts require maintainer resolution.\n",
        "spec.md": "`timeout` is an integer duration in milliseconds.\n",
        "config.schema.json": "{\"properties\":{\"timeout\":{\"type\":\"integer\",\"description\":\"Duration in seconds\"}}}\n"
      }
    },
    {
      "id": 6,
      "prompt_type": "anti_trigger",
      "prompt": "Mine invariants for this serializer and add round-trip property tests over generated inputs.",
      "expected_output": "Routes to invariant-miner because the task searches an input space rather than reconciling claims across artifacts.",
      "expectations": [
        "Uses invariant-miner rather than spec-reconciler",
        "Does not create a cross-surface claim graph"
      ]
    },
    {
      "id": 7,
      "prompt_type": "anti_trigger",
      "prompt": "Update this one known README sentence from port 8080 to port 8081.",
      "expected_output": "Makes the direct documentation edit without invoking spec-reconciler because the discrepancy and authority are already supplied.",
      "expectations": [
        "Does not create CLAIMS.json or a reconciliation report",
        "Edits the known sentence directly"
      ]
    },
    {
      "id": 8,
      "prompt_type": "anti_trigger",
      "prompt": "Review this single OpenAPI document for syntax errors and invalid references.",
      "expected_output": "Validates the supplied artifact directly without invoking spec-reconciler because no competing surface is involved.",
      "expectations": [
        "Does not invent comparison surfaces",
        "Runs an appropriate direct schema validation"
      ]
    },
    {
      "id": 9,
      "prompt": "Reconcile this API document with itself and its client. The same response field has two incompatible types in separate schema locations; do not discard either claim.",
      "expected_output": "Extracts both same-surface claims with distinct provenance and classifies an intra-surface conflict before cross-surface authority resolution.",
      "expectations": [
        "Preserves both claims from the same surface and key",
        "Anchors each claim to its own path:line",
        "Classifies intra-surface-conflict",
        "Marks it non-fixable pending resolution",
        "Does not choose a value by occurrence order"
      ],
      "fixture_files": {
        "AGENTS.md": "api.json is the primary API contract.\n",
        "api.json": "{\"components\":{\"schemas\":{\"User\":{\"properties\":{\"id\":{\"type\":\"string\"}}},\"UserResponse\":{\"properties\":{\"id\":{\"type\":\"integer\"}}}}}}\n",
        "client.py": "UserId = str\n"
      }
    }
  ]
}
